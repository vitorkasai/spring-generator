// Deve gerar erro de simbolo nao identificado
entidades = [
    Pessoa = {
        nome: String
        email: String,
        idade: Long
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{nome}"
        POST "/pessoa"
        PUT "/pessoa/{nome}"
        DELETE "/pessoa/{nome}"
    }
]
