// Deve gerar erro identificador inválido (não pode iniciar com número)
entidades = [
    Pessoa = {
        id: Long
        1nome: String
        email: String
        idade: Long
    }
]

2endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
]
