// Deve gerar erro identificador inválido (não pode iniciar com número)
1entidades = [
    Pessoa = {
        id: Long
        1nome: String
        email: String
        idade: Long
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
]
