// Deve gerar erro por conta da definição inválida de simbolo

#DEFINE ENTIDADES
entidades = [
    Pessoa = {
        id: Long
        nome: String
        email: String
        idade: Long
        //idade @#!
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
]
