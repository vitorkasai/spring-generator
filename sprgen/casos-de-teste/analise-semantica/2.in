// Deve retornar erro de identificador não declarado anteriormente
entidades = [
    Pessoa = {
        nome: String
        cpf: String
        email: String
        idade: Long
    }
    Carro = {
        modelo: String
        placa: String
        ano: Long
        cpfDono: String
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{cpf}"
        POST "/pessoa"
        PUT "/pessoa/{cpf}"
        DELETE "/pessoa/{cpf}"
    }
    Carro = {
        GET "/carro/{renavam}"
    }
]
