// Deve retornar erro de identificador não declarado anteriormente
entidades = [
    Pessoa = {
        id: Long
        nome: String
        cpf: String
        email: String
        idade: Long
    }
    Carro = {
        id: Long
        modelo: String
        placa: String
        ano: Long
        cpfDono: String
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
    Carro = {
        GET "/carro/{renavam}"
    }
]
