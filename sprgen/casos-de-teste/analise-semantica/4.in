// Deve retornar erro de endpoint já declarado anteriormente no programa
entidades = [
    Pessoa = {
        id: Long
        nome: String
        cpf: String
        email: String
        idade: Long
    }
    Carro = {
        id: Long
        modelo: String
        placa: String
        ano: Long
        cpfDono: String
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
    Pessoa = {
        GET "/pessoa/{id}"
    }
]
