// Deve dar erro pois Carro e Endereco não contém identificadores
entidades = [
    Pessoa = {
        id: Long
        nome: String
        cpf: String
        email: String
        idade: Long
    }
    Carro = {
        modelo: String
        placa: String
        ano: Long
        cpfDono: String
    }
    Endereco = {
        rua: String
        bairro: String
        complemento: String
        numero: Long
        isResidencial: boolean
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
    Carro = {
        GET "/carro"
        GET "/carro/{id}"
        POST "/carro"
        PUT "/carro/{id}"
        DELETE "/carro/{id}"
    }
    Endereco = {
        GET "/endereco"
        GET "/endereco/{id}"
        POST "/endereco"
        PUT "/endereco/{id}"
        DELETE "/endereco/{id}"
    }
]
