// Deve gerar erro pois não houve o fechamento da lista ( ] ) na linha 17
entidades = [
    Pessoa = {
        id: Long
        nome: String
        cpf: String
        email: String
        idade: Long
    }
    Carro = {
        id: Long
        modelo: String
        placa: String
        ano: Long
        cpfDono: String
    }

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
    Carro = {
        GET "/carro/{id}"
    }
]
