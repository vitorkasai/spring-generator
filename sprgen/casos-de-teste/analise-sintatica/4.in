// Deve gerar erro pela falta de fechamento da chaves na passagem de parametro na url " } "
entidades = [
    Pessoa = {
        nome: String
        email: String
        idade: Long
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{nome}"
        POST "/pessoa"
        PUT "/pessoa/{nome"
        DELETE "/pessoa/{nome}"
    }
]
