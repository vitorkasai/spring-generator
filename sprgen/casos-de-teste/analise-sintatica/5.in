// Deve gerar erro pois o tipo do atributo não foi informado
entidades = [
    Pessoa = {
        id: Long
        nome: String
        email:
        idade: Long
    }
]

endpoints = [
    Pessoa = {
        GET "/pessoa"
        GET "/pessoa/{id}"
        POST "/pessoa/{id}"
        PUT "/pessoa/{id}"
        DELETE "/pessoa/{id}"
    }
]
